# Monte-Carlo-Verification-of-Laplace-s-Rule-of-Succession

This Python script was created to test Laplace's Rule of Succession, which caught my interest while reading the "Bayes's Rule" chapter in the book *Algorithms to Live By* [1,2,3].

Laplace's Rule provides a way to estimate the probability of a future event given past observations, especially when prior information is limited.

As an example case of the problem, the code demonstrates how to estimate the probability of winning in a raffle (or any binary event) **when the true probability is unknown**. The code:

1. **Simulates possible “raffle machines”** with unknown win probability.
2. **Filters only the machines** that could have produced the observed results (wins and losses).
3. **Predicts the next outcome** by averaging over these plausible machines.

This method shows how Laplace’s Rule of Succession emerges naturally, purely from simulation, without assuming the Beta distribution.

---

## Theoretical Equation

The probability of winning the next raffle, according to Laplace, is:

`P(next win) = (w + 1) / (w + l + 2)`

Where:

- `w` = number of observed wins
- `l` = number of observed losses

This formula gives a smoothed estimate, accounting for the uncertainty in the true win probability.

---

## How It Works

1. Input:
   - `w` = number of observed wins
   - `l` = number of observed losses
2. Randomly guess a possible win probability `p`.
3. Simulate the observed sequence of wins/losses using `p`.
4. Accept `p` if it matches the observed results exactly.
5. Simulate one additional “next draw” for this accepted `p`.
6. Repeat until enough accepted worlds are collected.
7. Compute the fraction of next draws that are wins → estimated probability.

---

## Example Usage

```python
w, l = 3, 1
estimate = reverse_monte_carlo(w, l)
print("Estimated probability of next win:", estimate)
```

---

## Authorship

1. The underlying solution (Laplace’s Rule of Succession) was devised by Pierre-Simon Laplace, based on earlier work by Thomas Bayes.
2. The reverse Monte Carlo implementation (the Python code) was conceptualized by Murat Cihan Sorkun.
3. The original code and explanations were generated by (GPT-5 mini) on 05-04-2026.
4. The code has been revised and validated by Murat Cihan Sorkun to ensure correctness, clarity, and alignment with Laplace’s Rule of Succession.

---

## References
[1] Kleinberg, Brian, et al. *Algorithms to Live By: The Computer Science of Human Decisions.* Henry Holt and Co., 2016.

[2] Bayes, Thomas. *An Essay towards solving a Problem in the Doctrine of Chances.* Philosophical Transactions of the Royal Society of London, 1763.  

[3] Laplace, Pierre-Simon. *Théorie analytique des probabilités. 1812.
